function nbYear(p0, percent, aug, p) {
    let count=0;
    do{
        p0=p0+p0*(percent/100)+aug;
        count++;
        
    }
    while(p0<p)
   
    return (percent/100);
}
console.log(nbYear(1500, 5, 100, 5000))